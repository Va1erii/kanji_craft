name: kanji_craft_client
description: "Kanji Craft client app for learning kanji."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

resolution: workspace

dependencies:
  kanji_craft_core:
    path: ../../packages/core
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  get_it: ^8.0.3
  supabase_flutter: ^2.12.0

dev_dependencies:
  build_runner: ^2.11.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  freezed: ^3.2.4

flutter:
  uses-material-design: true
